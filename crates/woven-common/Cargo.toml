[package]
name        = "woven-common"
version     = "0.1.0"
edition     = "2021"
description = "Shared drawing primitives and text renderer for woven-shell"

[dependencies]
tiny-skia = { workspace = true }
fontdue   = { workspace = true }
tracing   = { workspace = true }
